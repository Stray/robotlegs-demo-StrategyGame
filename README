A simple strategy game engine built with robotlegs, without the use of a game-controller or 'god object'.

That's the plan anyway. I'll post progress here as I work through it so that the commits roughly capture the workflow.